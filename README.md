# CapstoneProject
Team based project for the EduFund platform. 

## Netlify deployment

See `Docs/Netlify.md` for step-by-step instructions to build and deploy the frontend to Netlify. The frontend is located in the `Frontend/` folder and the build output is `dist`.
